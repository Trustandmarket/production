<!-- Google tag (gtag.js) -->
{% set is_register_route = app.request is defined and app.request.attributes.get('_route') == 'app_register' %}
{% set is_google_tag_enabled = is_register_route or (environnement is defined and environnement == 'prod') or app.environment == 'prod' %}
{% if is_google_tag_enabled %}
<script async src="https://www.googletagmanager.com/gtag/js?id=G-ZEETE77ZKR"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-ZEETE77ZKR');

  {% if is_register_route %}
  // Helper to delay navigation until the conversion event is sent.
  function gtagSendEvent(target) {
    var callback = function () {
      if (target && typeof target.submit === 'function') {
        target.submit();
      } else if (typeof target === 'string') {
        window.location = target;
      }
    };

    gtag('event', 'ads_conversion_signup_trust', {
      'event_callback': callback,
      'event_timeout': 2000
    });

    return false;
  }
  {% endif %}
</script>
{% endif %}
